<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liaoyuan.web.dao.ICoalDao">

    <select id="countCoalData" parameterType="DataBean" resultType="java.lang.Integer">
        select count(*) from ${table}  where 1=1 
        <if test="name!=null and name!=''">
            and name like "%"#{name}"%"
        </if>
    </select>
    <select id="getTableData" parameterType="DataBean" resultType="DataBean">
        select * from ${table}   where 1=1 
        <if test="name!=null and name!=''">
            and name like "%"#{name}"%"
        </if>
         order by id desc limit ${iDisplayStart},${iDisplayLength}
    </select>

</mapper>
